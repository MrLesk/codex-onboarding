# Codex Onboarding Skill

This is an experimental skill that teaches you how to use Codex and the best practices for working with AI Agents.

## Installation

```bash
npx skills add mrlesk/codex-onboarding
```

For the best results you should set `default_mode_request_user_input = true` in `~/.codex/config.toml`